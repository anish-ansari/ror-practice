<%# Displays entered articles %>
<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>
<p>
  <strong>Text:</strong>
  <%= @article.text %>
</p>
<%# Displays all comments %>
<h2>Comments</h2>
<%# <%= render @article.comments %>
<%= render @article.comments %>

<%# Form to add new comment %>
<h2>Add a comment:</h2>
<%= render 'comments/form' %>
<%= link_to 'Go back', articles_path %> |
<%= link_to 'Edit', edit_article_path(@article) %>
